<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CfgTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CfgTest" parent="Base.Theme.CfgTest" />

    <style name="FullScreenDialog" parent="Theme.CfgTest">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <style name="FullScreenDialog.LoadingDialog">
        <item name="android:windowBackground">@color/loading_dialog_full_screen_bg</item>
    </style>

</resources>